# Django chỉ nạp file .env tại thư mục gốc dự án, không nạp backend/.env.
# Dùng ../.env.example làm mẫu local; dùng ../.env.production.example khi triển khai.
# Không sao chép file này thành backend/.env vì các biến trong đó sẽ không được nạp.
