# Pin the rule set explicitly so CI is stable across ruff versions
# (ruff's default rule set has changed over time).
#   E4/E7/E9: pycodestyle errors
#   F:        pyflakes
[lint]
select = ["E4", "E7", "E9", "F"]