{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip

	handle_path /static/* {
		root * "{$STATIC_ROOT}"
		file_server
	}

	handle_path /media/* {
		root * "{$MEDIA_ROOT}"
		file_server
	}

	handle {
		reverse_proxy 127.0.0.1:8000
	}
}
